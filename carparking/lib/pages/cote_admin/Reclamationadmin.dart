import 'package:flutter/material.dart';

class ReclamationAdmin extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Réclamation'),
      ),
      body: Center(
        child: Text('Page pour les réclamations'),
      ),
    );
  }
}
